{
  "name": "server",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build": "bun build src/server.ts --minify --outfile=./dist/server.js --target bun",
    "prod": "bun ./dist/server.js",
    "generate": "drizzle-kit generate",
    "push": "drizzle-kit push"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.7",
    "drizzle-orm": "^0.44.7",
    "postgres": "^3.4.7"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "hono": "^4.10.8",
    "openai": "^6.9.1",
    "zod": "^4.1.13"
  }
}
