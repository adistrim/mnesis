{
  "name": "server",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/dev/server.ts",
    "dev:build": "bun build src/dev/server.ts --outdir=dist --minify --target=bun",
    "dev:preview": "bun run ./dist/server.js",
    "generate": "drizzle-kit generate",
    "push": "drizzle-kit push"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.7",
    "drizzle-orm": "^0.44.7",
    "postgres": "^3.4.7"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "hono": "^4.10.8",
    "openai": "^6.9.1",
    "zod": "^4.1.13"
  }
}
