{
    "name": "mnesis",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "dev": "bun run --workspaces dev",
        "build": "bun run --workspaces build",
        "prod": "bun run --workspaces prod"
    }
}
